name: ephemeral
description: Weak data types like WeakValueMap and WeakKeyMap
version: 0.2.0
repository: https://github.com/mcmah309/ephemeral

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
